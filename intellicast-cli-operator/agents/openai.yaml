interface:
  display_name: "IntelliCast CLI Operator"
  short_description: "Inspect and edit IntelliCast projects via CLI."
  default_prompt: "Use $intellicast-cli-operator to inspect an IntelliCast project, choose the smallest supported intellicast-cli command, and validate persisted state with inspect before any screenshot or export."
